uniform highp float contrast = 0.9;
uniform highp float minus = 0.5;
uniform highp float tranq_power = 0.8;
uniform sampler2D SCREEN_TEXTURE;



void fragment() {
   highp vec2 coord = FRAGCOORD.xy * SCREEN_PIXEL_SIZE.xy;
   highp vec3 rgb = texture(SCREEN_TEXTURE, UV).xyz;
   highp vec4 Color = zTexture(UV);

   rgb = ((rgb - minus) * contrast + minus);
   Color = vec4(rgb,1.0);
   COLOR = mix(COLOR,vec4(rgb,1.0),min(tranq_power,1.0));
}
